/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * User Manual available at https://docs.gradle.org/5.5.1/userguide/tutorial_java_projects.html
 */

plugins {
    // Apply the java plugin to add support for Java
    id 'java'

    // Apply the application plugin to add support for building a CLI application
    id 'application'

    // Apply the application plugin to add support for test coverage using JaCoCo
    id 'jacoco'
}

jacocoTestReport {
    reports {
        csv.enabled false
        xml.enabled false
        html.enabled true
    }
    afterEvaluate {
        classDirectories.from = files(classDirectories.files.collect {
            fileTree(dir: it, 
                    exclude: [
                        '**/TwitterApp.class',
                        '**/TwitterAuth.class',
                        '**/AppConfiguration.class'
            ])
        })
    }
}

project.ext {
    configPath = "$rootProject.projectDir/config/"

    dropwizardVersion = '1.3.12'
}

repositories {
    // Use jcenter for resolving dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
}

dependencies {
    // Use Twitter4J
    implementation 'org.twitter4j:twitter4j-core:4.0.7'
    compile group: 'io.dropwizard', name: 'dropwizard-core', version: dropwizardVersion
    compile group: 'org.slf4j', name: 'slf4j-api', version: '1.7.26'
    testCompile group: 'io.dropwizard', name: 'dropwizard-testing', version: dropwizardVersion
    testImplementation "org.mockito:mockito-core:2.+"
    testImplementation('org.junit.jupiter:junit-jupiter:5.5.1')
}

test {
    useJUnitPlatform()
    testLogging {
        events "passed", "skipped", "failed"
    }
}


application {
    // Define the twitter_bootcamp class for the application
    mainClassName = 'twitter_bootcamp.TwitterApp'
}

run {
    args 'server', configPath + 'app_config.yml'
}

sourceCompatibility = 1.8